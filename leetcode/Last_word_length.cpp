#include "Last_word_length.h"
#include <string>

using namespace std;

#include <iostream>

int Last_word_length::length(const std::string& str_) {
    
    // "Hello World " = 5 because "World".length() == 5

    int lengthOfWholeString = str_.length();
    int current_count = 0;
    int next_count = 0;

    // for(int i = 0; i < lengthOfWholeString; i++){
        
    // }

    return -1;
}